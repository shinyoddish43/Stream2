# Settings for the six7 hub (t.six7.pw). No secrets here.
# The hub's Caddy has this fixed address on six7_studio-edge.
TRUST_PROXY=10.67.0.2
# authentik's forward auth names the signed-in user in this header.
AUTH_HEADER=X-Authentik-Username
AUTH_USERS=Oddish
LOGOUT_URL=/outpost.goauthentik.io/sign_out
